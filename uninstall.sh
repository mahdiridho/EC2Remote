sudo npm uninstall -g bower gulp # these are used by other projects
sudo apt-get remove npm nodejs nodejs-dev nodejs-legacy nodejs-dbg  # these are used by other projects
sudo rm -rf ./* # these are in the git repo

